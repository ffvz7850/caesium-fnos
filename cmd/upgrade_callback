#!/bin/bash
# Caesium upgrade post-hook
chmod 755 "${TRIM_APPDEST}/caesiumclt" 2>/dev/null || true
chmod 755 "${TRIM_APPDEST}/server.py" 2>/dev/null || true
chmod +x "$(dirname "$0")"/* 2>/dev/null || true
exit 0
